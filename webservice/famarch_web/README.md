### Datenbank
+ Postgres gedacht als Docker-Container
+ create-script vorhanden
+ test-daten vorhand
+ DB-Benutzername in hibernate.cfg.xml noch nicht definiert
+ DB-Passwort in hibernate.cfg.xml noch nichtdefiniert

### Webservice
+ Als Spring-Boot application
+ Hibernate als DB-Connector
+ Entitys mit Annotations umgesetzt
+ APIs in ArchivController (RestController) definiert

### Benutzer-Management
+ noch nicht umgesetzt
+ API-Calls werden noch nicht auf Berechtigung ueberprüft
+ exisiterende API um Benutzerdaten an Frontend weiterzugeben
+ Verwaltung sollte wahrscheinlich im Backend umgesetzt weden